spring:
  application:
    name: spark-admin
  security:
    user:
      name: 'admin'
      password: 'admin'
  cloud:
    nacos:
      ##### nacos(注册中心和配置中心)地址
      server-addr: 116.63.161.163:8848
      username: nacos
      password: nacos
      config:
        file-extension: yml
#      discovery:
#        metadata:
#          user.name: ${spring.security.user.name}
#          user.password: ${spring.security.user.password}